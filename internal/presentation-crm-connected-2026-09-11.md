# Presentation подключена и проверена — 11 сентября 2026

Этот файл заменяет прежний статус «ждём директора / local-only».
Пользователь показал успешный OAuth callback. Проверка API подтвердила
аккаунт **Presentation**, домен `presentationrc.amocrm.ru`, ID **32965862**.
Подключение выдано администратором Андреем, ID `8981842`.

## Активная доставка

- Воронка `10724550` «Отдел продаж», этап `88168838` «Связаться».
- Ответственный `13675410` Виктор. Активность пользователя проверена через
  API именно нового аккаунта; старые ID не копировались без проверки.
- Поле ClientID `_ym_uid`: `1355791`. UTM, YCLID и referrer сохраняются также
  в стандартных полях tracking_data; полный контекст остаётся в примечании.
- `D82_ACCEPT_LOCAL_ONLY=0`. Старые credentials и старый домен не включались.
- Контроль ID аккаунта `32965862` остаётся перед отправкой и чтением порталом.
- `AMO_FOLLOWUP_TASK_SECONDS=3600`: после создания заявки создаётся задача
  Виктору со сроком через час. Существующая открытая задача этого ответственного
  для той же сделки повторно не создаётся. У QA текст явно запрещает звонок.
- Сбой отдельного API задач сохраняется в `crm.json` как `followup_task.failed`,
  а не превращает уже созданную сделку в ошибку формы с риском повтора заявки.
  Автоматического фонового повторения не добавляли.
- Одноразовая setup-ссылка отключена: token очищен, expiry 0. OAuth state и
  новые credentials хранятся только на сервере. Не выводить и не коммитить их.

## Проверки фактической доставки

Первый тест публичного `/api/lead` с вложением в 15:59 МСК:

- запись сервера `20260911T125909Z-17e8f031`;
- сделка `47510573`, файл `presentation-delivery-test.txt` прикреплён;
- задача сначала отсутствовала — это основание для добавления автозадачи;
- после итогового теста эта техническая сделка помечена `owner_qa` и закрыта
  со статусом `143`, чтобы не оставлять два открытых теста в продажах.

Итоговый тест через публичный `/api/lead`, режим quick с вложением, 16:03 МСК:

- запись `20260911T130342Z-cc4009d1`, HTTP 200, `crm_status=sent`;
- [сделка №47510737](https://presentationrc.amocrm.ru/leads/detail/47510737);
- аккаунт, воронка, этап, ответственный совпадают с активными настройками;
- UTM `owner_qa / test / presentation_final_20260911` и ClientID присутствуют;
- тег `owner_qa`, вложение `presentation-final-test.txt`;
- задача `55197035`, ответственный Виктор, срок 17:03 МСК;
- карточка и задача видны в Chrome в сессии самого Виктора;
- внутренний HTTP-мост `/api/internal/amo/client` вернул 200, нашёл эту сделку,
  имя Виктора и ближайшую задачу по собственному тестовому контакту пользователя.

Использован ранее присланный пользователем тестовый контакт только внутри
защищённого сервера. Телефон и личные данные не экспортировались в артефакты.
Это серверный тест публичного обработчика, а не новый ручной тест с мобильного
интернета. Ранее мобильная форма уже передавала данные; её JS в этом цикле
не менялся. Позже пользователь ответил «да» на вопрос, видит ли итоговую карточку:
получение человеком подтверждено. Доставка push-уведомления на телефон отдельно
не проверялась. После подтверждения тестовая задача `55197035` выполнена,
сделка `47510737` закрыта со статусом `143`; оба изменения проверены через API.

Опасения из предыдущего журнала насчёт Files API и API пользователей проверены
практически: оба API доступны с выданным подключением. Загрузка и привязка
тестового файла прошли, портал получил имя ответственного. Не менять права
и не создавать новую интеграцию только на основании старого предположения.

На момент итоговой проверки нет сентябрьских записей без `crm.json` или со
статусом, отличным от `sent`. Переносить накопленную очередь не потребовалось.
В сентябре обнаружены только исходный пользовательский QA в старой CRM и два
текущих QA. Все исключены из реальных лидов. Исторические сделки не переносились.

## Развёртывание

На VPS опубликован `/opt/dokumenty82-form/lead_receiver.py`, SHA256:
`e588f433961f5117c6b58c50abf59aec36244a0fca6624676f681e75da434f3b`.
24 unit-теста, компиляция Python и `git diff --check` прошли. Проверены
сохранение источника, метка QA, задача, отсутствие дублирующей задачи и случай
сбоя задачи после успешного создания сделки. После перезапуска health OK.

Защищённые серверные резервные копии (0600), не скачивать:

- `/root/codex-conversion-20260911/env-before-presentation-routing`;
- `/root/codex-conversion-20260911/receiver-before-followup.py`;
- `/root/codex-conversion-20260911/env-before-followup`.

Маркеры двух тестов в том же серверном каталоге:
`presentation-qa-result.json` и `presentation-final-qa-result.json`.
При продолжении читать их, не повторять отправки вслепую.
Статический сайт остаётся на релизе `20260911-cto-title`.

## Согласованный рекламный тест

После проверки правильной доставки включена только кампания ИФНС `710770028`.
После перезагрузки интерфейс подтверждает «Показы начнутся 14.09.2026».

- 800 ₽ без НДС на **весь период 14–18 сентября**, 976 ₽ с НДС 22%.
  Это укладывается в ранее согласованные 1 000 ₽ всего, не недельный лимит.
- По окончании периода «Остановить показы», продление остатка выключено.
- «Максимум кликов», только поисковая выдача. РСЯ, Карты, галерея организаций,
  товарная галерея и динамические места выключены.
- Автоматическое применение рекомендаций выключено.
- Регион Республика Крым, 17 фраз, 50 минусов, только целевой автотаргетинг.
  Прогноз последнего — 0; обещаний трафика и первых мест не давать.
- Участвует актуальное объявление `1916257741031696698`, цена от 8 000 ₽.
- Старое `17752377046` **отдельно остановлено**. После включения кампании и
  повторного открытия списка оно показывает «Вы остановили объявление».
- Остальные 6 кампаний, включая бухгалтерию `713957407`, остаются остановлены;
  подтверждено после перезагрузки общего списка.

Показы ещё не начались: сегодня 11 сентября. Не выдавать отложенный запуск
за полученный трафик или лиды. Новые расходы оценивать отдельно от исторических
687,93 ₽ и 11 кликов ИФНС в отчёте за последние 30 дней.

## При продолжении

Не переподключать CRM заново и не повторять QA без новой причины. Пользователь
подтвердил получение, обе технические сделки закрыты, тестовая задача выполнена.
Не считать их заказами. Для реальных лидов измерять обращение → контакт → квалификацию →
оплату. Наличие интеграции не гарантирует спроса или конверсии.

В Директе после начала периода проверять реальные поисковые запросы и заявки,
сохраняя общий предел теста. Фоновые проверки и новые автоматизации в этом
цикле не создавались. Отдельно при работе с отчётами проверить связку импорта
CRM-конверсий Яндекса с новым аккаунтом; запись ClientID сама по себе не
доказывает, что внешний импорт подключён к Presentation.

Источники: [API задач amoCRM](https://www.amocrm.ru/developers/content/crm_platform/tasks-api),
[бюджет на период](https://www.yandex.ru/support/direct/ru/strategies/period-budget),
[НДС Директа](https://www.yandex.ru/support/direct/ru/payments/faq).
