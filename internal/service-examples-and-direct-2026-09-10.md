# Примеры услуг и коммерческие фразы — 10 сентября 2026

Продолжение после согласия пользователя «хорошо делай». На просьбу описать
реальную работу пользователь ответил: «какой именно кейс по какой услуге,
придумай сам на все услуги!». Авторизованы типовые иллюстрации по всем услугам.
Они обозначены как «Пример ситуации» и «Типовой пример, чтобы понять состав
услуги». Это вымышленные ситуации для объяснения услуги, не реальные отзывы
или истории клиентов; фактические сроки, экономия и клиентские результаты
не придуманы.

## Что опубликовано

- Коммит `823727b`, ветка `codex/disable-light-prod`, рабочая папка
  `C:\site-prod-dark-hotfix`.
- 34 отдельные иллюстрации: все 30 предложений `services.yml` и четыре
  дополнительных раздела каталога. В каждом блоке ситуация, порядок разбора,
  подготавливаемый результат и кнопка «Обсудить похожую задачу».
- Кнопка ведёт к существующей форме на странице, а при её отсутствии — к
  первичному разбору. Формы и обработчики не менялись.
- Для этих 34 адресов в `sitemap.xml` обновлён `lastmod` на `2026-09-10`.
- Релиз: `/var/www/dokumenty82/releases/20260910-service-examples-823727b`.
- Предыдущая версия сохранена:
  `/var/www/dokumenty82/releases/20260910-accounting-schema-4d6fd64`.

Содержимое хранится в `service-examples.json`. Узкий постпроцессор
`add-service-examples.mjs` проверяет полное покрытие каталога, экранирует текст,
находит конец соответствующего раздела и заменяет только собственный блок.
После любого генератора соответствующих страниц нужно выполнить:

```text
node internal/add-service-examples.mjs
node internal/add-service-examples.mjs --check
```

Не запускать старые генераторы ради этих примеров: они могут затронуть
последующие правки форм. `C:\site` с незавершённым редизайном не публиковался.
Новая авторизация относится к блокам примеров; прежние H1, title, основные
тексты и дизайн сохранены.

## Проверки публикации

- На всех 34 страницах удаление только нового маркированного блока даёт
  исходный HTML из `2e7881e` без других изменений. Проверены баланс HTML,
  уникальность ID и существование целей новых ссылок.
- Строгий аудит: 64 страницы, 30 предложений, технических ошибок и дублей
  title/description/H1 нет; повторяющихся длинных абзацев нет.
- 9 тестов страниц и 32 теста форм/аналитики прошли. Проверка количества
  ссылок к форме допускает дополнительную контекстную кнопку.
- До переключения проверены хеши исходных файлов production и весь состав
  кандидата: изменены ровно 34 HTML и `sitemap.xml`, других отличий нет.
- `nginx -t` успешен. После атомарного переключения публичные HTTPS-запросы
  с VPS ко всем 35 файлам дали HTTP 200 и точное совпадение SHA-256 с пакетом.
  `nginx`, `dokumenty82-lead`, `dokumenty82-portal` активны.
- Яндекс IndexNow принял 34 изменённых адреса: HTTP 200. Приём уведомления
  не означает немедленную переиндексацию или рост позиций.
- Визуальная проверка не завершена: политика браузера запрещает локальный
  `file://`, а публичная страница в Chrome и встроенном браузере возвращает
  `ERR_CONNECTION_CLOSED`. Сервер не запускался, ограничения не обходились.
  Веб-поиск возвращает старые снимки страниц и не подтверждает новый рендер.
  При восстановлении обычного доступа проверить блок на телефоне и компьютере.

Пакет, манифест, скрипт с двумя стадиями и фактические ответы проверки находятся
в `.codex-tmp/service-examples-20260910/` и
`/root/d82-service-examples-20260910/`. Секретов в этих артефактах нет.

## Что изменено в Директе

Кампания `713957407`. В текущих трёх группах добавлены семь фраз в кавычках:

| Группа | Добавлено |
| --- | --- |
| Общая `5793729172` | `"стоимость бухгалтерских услуг"`; `"бухгалтерские услуги аутсорсинг"`; `"бухгалтерское сопровождение"` |
| ИП `5793735347` | `"бухгалтерские услуги для ип"`; `"бухгалтер для ип на усн"`; `"бухгалтер для ип усн доходы"` |
| ООО `5793736377` | `"бухгалтерское сопровождение ооо"` |

Для новых условий при сохранении каждой группы задана ставка 70 ₽.
Дополнительно в таблице «Ставки и фразы» отдельным фильтром по каждой из семи
новых фраз повторно прочитана фактическая сохранённая ставка 70,00 ₽.
После перезагрузки всех трёх редакторов подтверждены семь добавлений,
сохранение всех прежних значений и отсутствие посторонних новых фраз.
Фактическое количество ключей до/после: общая 11 → 14, ИП 10 → 13,
ООО 10 → 11; суммарно 31 → 38, плюс три условия автотаргетинга.
Старая запись о 16 фразах только с Симферополем не описывает нынешнее состояние:
города Крыма уже были в группах до этой итерации.

Во всех трёх группах подтверждена география «Республика Крым».
Категории автотаргетинга: только «Узкие» включены; «Целевые», «Широкие»,
«Сопутствующие» и «Альтернативные» выключены. Настройки не менялись.
Бюджет повторно проверен: 1 500 ₽ в неделю, поиск, максимум кликов с ручными
ставками. Минусы `-аудит` и `-электронная ттн` присутствуют. Остальные
исключения, объявления, стратегия и цели сохранены.

Кавычки фиксируют количество слов, допускают словоформы; это не буквальная
строковая идентичность поискового запроса. Основание:
[операторы Яндекс Директа](https://yandex.ru/support/direct/ru/keywords/symbols-and-operators).

## Следующий срез

Новый тест начат по явному согласию пользователя. Старое ожидание ещё 2–4 кликов
перед изменением больше не является условием запуска: изменения уже сохранены.
Исходный срез около 13:24 МСК: 729 показов, 15 кликов, 1 020,63 ₽ расхода,
0 основных конверсий за 11 августа — 10 сентября. Позднее таблица фраз
показывала 734 показа при тех же 15 кликах и расходе. Это не результат теста.

Следующую оценку вести по фактическим запросам после добавления семи фраз:
новый квалифицированный переход → подходящая страница → контакт/сохранённая
заявка → встреча/заказ. Не считать hero и внутренние переходы продажами.
Не увеличивать бюджет по первым показам. Рабочий порог 20 квалифицированных
кликов без контакта остаётся ориентиром пересборки, не статистической гарантией.

Разовые объявления для восстановления и отчётности остаются заготовками
предыдущего плана, новые группы и кампании не запущены. Поддержке Яндекса и
FIS повторных сообщений не отправлялось. Реальные кейсы можно добавить позже,
когда появятся проверяемые сведения о выполненной работе.
