# Футер — 14 сентября 2026

Опубликован финальный релиз `20260914-footer-wide`, CSS `/assets/footer.css?v=2026091403`.
Пользователь одобрил компактный вариант, затем попросил выровнять «Разделы»
по центру над меню, сделать футер шириной с шапку, увеличить Times New Roman
и расположить разделы в три столбца. Эти уточнения выполнены.

## Оформление

- Ширина совпадает с шапкой: до 1680 px на больших экранах, общие мобильные края.
- Основной текст и ссылки — 16 px, Times New Roman; подписи — 14 px.
- Три столбца ссылок, заголовок по центру; на телефонах до 460 px — два столбца.
- Прозрачная заливка, видимый фон `route-digital-wave.png` и один плавный блик
  через весь футер. `prefers-reduced-motion` отключает блик.
- Настоящий iframe Яндекса 150 × 50, организация `1302424560`.
  В браузере видны рейтинг 4,7 и звёзды; рейтинг не захардкожен в разметке.
- Сохранены телефон, email, Telegram, MAX, реквизиты, политика и оферта.
- `overflow: clip` предотвращает внутреннюю прокрутку футера при переходе
  к iframe: у первой итерации из-за размеров блика содержимое сдвигалось вверх.

Общий шаблон — `internal/compact-footer.html`, применение —
`internal/apply-compact-footer.mjs`; подключён к `internal/refresh-site.mjs`.
Полный генератор не запускался. Обновлены 67 публичных HTML и новый CSS.

## Проверки

- Побайтовое сравнение после исключения футера и его stylesheet подтверждает,
  что основное содержимое, метаданные, формы и скрипты всех 67 HTML сохранены.
- Повторное применение шаблона: 0 изменений; `git diff --check` пройден.
- Семь существующих тестов оферты пройдены, включая повторную генерацию.
  Использован Python из bundled runtime с lxml.
- Аудит 64 страниц sitemap: 0 технических ошибок, нет повторов title/description/H1.
- Публичная HTTP/SHA256-проверка 68 путей пройдена. Старый адрес
  `/blog/obnovleniya-fns/` перенаправляет на `/novosti/`; сверялся файл назначения.
- В браузере финальный футер и шапка при viewport 1920 px имеют ширину
  1680 px и одинаковый X. Высота футера около 279 px, текст 16 px, три столбца.
- Дополнительно проверены 1280/768/390/320 px: ширина совпадает с шапкой,
  нет горизонтальной прокрутки страницы и переполнения текста футера.
  Desktop просмотрен визуально; mobile раскладка проверена по DOM-геометрии.
- Проверены настоящий баннер и работа анимации. Временная ширина браузера сброшена.

Публичный SHA256 `footer.css`:
`e3454bc85951fe09d4c3c191ccac8bba167041aa949887946ce0649bbbfb0d06`.

## Публикация

Текущий symlink: `/var/www/dokumenty82/releases/20260914-footer-wide`.
Предыдущие итерации `20260914-footer-compact`, `20260914-footer-compact-fix`
и исходный `20260911-team-glass-motion` сохранены. Каждый деплой проверил
полный состав файлов и ровно 68 разрешённых изменений; Nginx и сервисы активны.
Артефакты финала: `.codex-tmp/footer-wide-20260914/` и `/root/footer-wide-20260914/`,
включая серверные `deployment-result.json` и `public-verification.json`.

QA-переходы: `utm_source=owner_qa`, `utm_medium=test`, кампании
`footer_20260914` / `footer_20260914_verify`; исключать из клиентского трафика.
CRM Presentation, рекламные настройки и формы не менялись; заявки не отправлялись.

SSH 14 сентября работает через прежний `ssh-direct.py` с интерфейсом 5
и локальным IPv4 `192.168.8.95`. Адрес `.93` устарел. Сетевые настройки не меняли.
