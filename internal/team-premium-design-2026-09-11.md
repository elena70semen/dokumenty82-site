# Оформление команды по референсу календаря — 11 сентября 2026

Пользователь попросил сделать карточки премиальнее, ориентируясь на второй
скриншот — существующий блок налогового календаря. Применена его визуальная
основа: глубокий тёмно-синий фон, тонкие золотые линии, цифровая графика,
плашки направлений и крупные типографические акценты справа.

Шесть имён, должности и полные описания сохранены. Виктор — только CTO.
Для Виктора и Екатерины показано «25+», для Алдын-Сай «20+» по ранее
подтверждённому пользователем опыту. У Андрея, Виктории и Марии — инициалы;
не добавлены неподтверждённые цифры, достижения или фотографии.

Карточки получили крупные имена, отдельную строку должности, более свободные
отступы и тонкую линию над описанием. На широком экране две колонки, до 700 px
одна. Декоративные инициалы и числа скрыты от accessibility tree; опыт также
остаётся в тексте. Каждая карточка связана со своим заголовком через aria-labelledby.
У секции задан scroll-margin для ссылки `#team` с фиксированной шапкой.

Стили находятся в новом `assets/team.css`, подключённом только к странице
«О проекте». Использован уже существующий `assets/brand/route-digital-wave.png`.
Глобальный `site.css`, остальные страницы, формы, CRM и реклама не изменялись.
Генератор `internal/apply-conversion-content.py` обновлён; повторная генерация
страницы даёт тот же HTML и не дублирует CSS-ссылку.

Опубликован релиз `/var/www/dokumenty82/releases/20260911-team-premium`.
Сохранён предыдущий `20260911-team-maria`. Deploy allowlist: ровно два публичных
файла — `o-proekte/index.html` и `assets/team.css`. Сравнение полной структуры
релизов подтвердило отсутствие изменений вне списка; новый CSS прежде отсутствовал.
Nginx и сервисы проверены перед переключением.

Публичные HTTP 200 и SHA256:

- HTML: `15cefb4e355c5359ddebf126853730baa21476716aba5aad069a9efea97b3b07`;
- CSS: `808703f2966c18b67d2c7d3447177477a6030561fb05b684f2d0260819a2124f`.

В браузере выполнены визуальные проверки при обычной ширине 1920 px и при
мобильной 390 px. Проверены границы текста всех шести карточек дополнительно
при 320 px: содержимое не обрезается, горизонтальной прокрутки страницы нет.
Псевдоэлементы с декоративными дугами намеренно выходят за границы и обрезаются
карточкой; это не переполнение текста. Временный viewport override сброшен.
Проверены валидность JSON-LD, уникальные ID, сохранение прежних описаний,
идемпотентность генератора и `git diff --check`.

Артефакты развёртывания: `.codex-tmp/team-premium-20260911/`.
Открытая вкладка с результатом сохранена пользователю.
