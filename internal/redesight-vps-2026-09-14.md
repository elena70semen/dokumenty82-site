# Отдельный VPS-предпросмотр redesight — 14 сентября 2026

По поручению пользователя белая версия без изображений размещена по IP:
[https://159.194.223.106/](https://159.194.223.106/).
HTTP также доступен.
DNS и основной сайт dokumenty82.ru не изменялись.

По последующему уточнению пользователя восстановлен исходный портрет девушки
в шапке AI-приёмной: `assets/images/ai-consultant-portrait.webp`. Изменено только
правило видимости в `assets/monochrome.css`; прочие изображения остаются скрыты.
CSS обновлён в действующем каталоге VPS с резервной копией предыдущего файла
в `/root/`. Портрет проверен в браузере на 320 и 1440 px; переполнения нет.
Локальный сервер для проверки не запускался.

Следующим обновлением восстановлена цветная интерактивная карта в «Контактах».
Вместо внешнего загрузчика используется его исходный iframe URL с тем же
идентификатором карты. Он разрешён существующей CSP; расширять доступ скриптов
не потребовалось. Удалён первый блок со скрытым фото офисной приёмной.
Страница теперь начинается с блока «Выберите удобный способ».

Пользователь подтвердил сохранение белой темы и попросил больше объёма:
общие панели и карточки получили светлый верхний край, внутренний блик,
нижнюю грань и более глубокие внешние тени, включая наведение и фокус.
Размеры, шрифты и содержимое карточек не менялись. Проверены главная,
«Контакты» и бухгалтерские услуги на 320, 768 и 1440 px, фокус клавиатуры
и режим уменьшения анимации; горизонтального переполнения нет.
Изменённые `assets/monochrome.css` и `kontakty/index.html` обновлены на VPS
с резервными копиями в `/root/`. Карта проверена с настоящими тайлами Яндекса
на широком и мобильном экране. Локальные HTTP-серверы не запускались.

- Публичный исходник: `0c9a4fa7c8dfe1b9f1b1512801199326b888f7f1`, ветка `redesight`.
- Сервер: Ubuntu 24.04.4 LTS. Установлен Nginx 1.24.0-2ubuntu7.17.
- Каталог: `/var/www/redesight/releases/0c9a4fa`.
- Конфигурация: `/etc/nginx/sites-available/redesight-preview.conf`, ссылка в
  `sites-enabled`. Репозиторный образец: `server/redesight-preview.nginx.conf`.
- Сервис Nginx активен и включён в автозагрузку. Порт 80 до работы был свободен.
  Существующие `bsc-local-dashboard` и Docker после размещения остаются active.
- Для IP выпущен доверенный сертификат Let's Encrypt; Nginx слушает также 443.
  Сертификат хранится в `/etc/letsencrypt/live/159.194.223.106/`, TLS 1.2/1.3.
  Certbot 5.8.0 установлен в отдельном окружении `/opt/redesight-certbot`.
  HTTP-проверка владения IP использует `/var/lib/redesight-acme`.
  `redesight-certbot-renew.timer` включён и проверяет продление каждые шесть
  часов с задержкой до 15 минут. Deploy hook после продления выполняет
  `nginx -t` и `systemctl reload nginx`. Образцы systemd unit-файлов — в `server/`.
- Загружены только 148 публичных файлов. `internal`, серверная логика, Git и
  данные доступа в веб-каталог не включались. Контрольные суммы всех файлов
  сверены на VPS с манифестом исходного коммита.
- `/api/*` возвращает 503 с ответом предпросмотра. Заявки, AI-чат и кабинет
  не подключены к реальным API/CRM; аналитика блокируется CSP. Исходные
  обработчики и формы в HTML/JS не менялись.
- Индексация предпросмотра закрыта заголовком `X-Robots-Tag` и `robots.txt`.

Проверено снаружи: 85 HTML/CSS/JS URL вернули HTTP 200 и совпали с SHA-256
исходников. Проверены также запрет индексации и ответы всех трёх API.
Главная просмотрена в браузере на 320, 390, 768 и 1440 px: белая схема,
нет видимых изображений и горизонтального переполнения. Проверка `nginx -t`
пройдена до перезагрузки конфигурации.

После включения HTTPS проверка TLS без отключения проверки сертификата
успешна. Главная, CSS белого стекла и кабинет вернули 200 и совпали с SHA-256
исходников. Заголовок запрета индексации, `robots.txt` и API предпросмотра
проверены также через HTTPS.
Тест `certbot renew --dry-run --run-deploy-hooks` прошёл: выпуск через
ACME webroot и перезагрузка Nginx после продления работают.

На текущем Windows-компьютере обычный сетевой маршрут возвращал пустой ответ
для HTTP, обрывал TLS handshake и закрывал SSH-соединение. Прямая привязка сокета к Wi-Fi
`192.168.8.95` работает. Для проверки браузера использован временный proxy
только на localhost; после проверки он остановлен. Системные маршруты и VPN
не менялись. Если IP не открывается через текущий VPN, проверить прямой доступ.
Локальный предпросмотр на 8184 остановлен по просьбе пользователя; локальный
proxy на 8186 тоже остановлен.

Данные доступа прочитаны из файла, переданного пользователем; они не включены
в Git, отчёты или публичный пакет. Протокол HTTP-проверки, манифест и снимки:
`.codex-tmp/redesight/vps-159.194.223.106/` и `.codex-tmp/redesight/vps-live-*.png`.
